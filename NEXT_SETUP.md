# Next.js Setup

This project was scaffolded by the Dark Software Factory.

If `create-next-app` wasn't run automatically, run:
```bash
npx create-next-app@latest medscribe-ai --typescript --tailwind --eslint --app --src-dir --import-alias '@/*'
```

Then merge the generated files with this directory structure.
